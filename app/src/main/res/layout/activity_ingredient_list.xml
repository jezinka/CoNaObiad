<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.projects.jezinka.conaobiad.activities.IngredientListActivity">

    <include
        android:id="@+id/ingredient_list_toolbar"
        layout="@layout/toolbar_main_app" />

    <android.support.design.widget.FloatingActionButton
        style="@style/floatingActionButtonAdd"
        android:id="@+id/add_ingredient_button"
        app:backgroundTint="@android:color/white"
        android:onClick="addIngredientButtonClick" />

    <ListView
        android:id="@+id/ingredient_list_view"
        style="@style/list"
        android:layout_below="@+id/ingredient_list_toolbar" />

</RelativeLayout>